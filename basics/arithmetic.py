# Operators
a = 2
x = 3.14
y = -4
z = 5


# result = round(x) #round funciton use to round of the decimal value
# result = abs(y)   #absolute is use to find the absolute value of numver (eg how far is a number form 0)
# result = pow(3 ,4) #3 to the power of 4 

# result = a + x # for addition             a += x print(a)     }
# result = a - x # for substrction          a -= x print(a)     }
# result = a * x # for multiplication       a *= x print(a)     }       variable a should be defined 
# result = a / x # for division             a /= 3 print(a)     }
# result = a % x # for to find remainder    a %= 2 print(a)     }
# result = a ** x # for power               a **= x print(a)    } 


#MIN and MAX function
max_value=max(x, y, z)
min_value=min(x, y, z)

print(min_value)
print(max_value)


